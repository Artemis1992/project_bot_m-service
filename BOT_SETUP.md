# 🤖 Инструкция по запуску Telegram-бота

## ⚡ Быстрый старт

**📖 [BOT_QUICK_START.md](BOT_QUICK_START.md)** - Запуск за 5 минут  
**📖 [BOT_REQUIREMENTS.md](BOT_REQUIREMENTS.md)** - Что требуется (краткая сводка)

---

## ⚡ Краткая сводка

**Для запуска бота вам нужно:**

1. ✅ **BOT_TOKEN** - получить от @BotFather в Telegram (5 минут)
2. ✅ **SERVICE_API_KEY** - сгенерировать ключ (1 минута)
3. ✅ **Запустить все сервисы** - requests, categories, approvals, files, reporting
4. ✅ **Применить миграции** - для Django-сервисов

**Время настройки:** ~10-15 минут (если сервисы уже запущены)

---

## Что нужно для запуска бота

### Обязательные требования

1. **BOT_TOKEN** - токен Telegram-бота (обязательно, реальный токен)
2. **SERVICE_API_KEY** - ключ для межсервисной аутентификации (тот же ключ во всех сервисах)
3. **Запущенные сервисы** - все остальные сервисы должны быть доступны:
   - `requests_service` (порт 8000)
   - `categories_service` (порт 8001)
   - `approvals_service` (порт 8002)
   - `files_service` (порт 8100)
   - `reporting_service` (порт 8200)

### Опциональные настройки

- **URL-ы сервисов** - если сервисы запущены не в Docker или на других портах
- **GOOGLE_SERVICE_ACCOUNT_JSON** - для работы с Google Sheets/Drive (если нужна синхронизация категорий)
- **GOOGLE_SHEET_ID** - для синхронизации категорий из Google Sheets

---

## 📋 Пошаговая инструкция

### Вариант 1: Запуск через Docker (рекомендуется)

#### Шаг 1: Получите BOT_TOKEN

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot` или `/token` для существующего бота
4. Скопируйте полученный токен

**📖 Подробная инструкция:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md#2-bot_token-telegram)

#### Шаг 2: Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
# Обязательные
BOT_TOKEN=ваш_токен_от_BotFather
SERVICE_API_KEY=ваш_api_ключ

# Опционально (если сервисы на других адресах)
CATEGORIES_SERVICE_URL=http://categories_service:8001/api
REQUESTS_SERVICE_URL=http://requests_service:8000/api
FILES_SERVICE_URL=http://files_service:8100
APPROVALS_SERVICE_URL=http://approvals_service:8002/api
REPORTING_SERVICE_URL=http://reporting_service:8200
```

#### Шаг 3: Запустите все сервисы

```bash
docker compose -f docker/docker-compose.yml up -d --build
```

#### Шаг 4: Примените миграции

```bash
# requests_service
docker compose -f docker/docker-compose.yml exec requests_service python manage.py migrate

# categories_service
docker compose -f docker/docker-compose.yml exec categories_service python manage.py migrate

# approvals_service
docker compose -f docker/docker-compose.yml exec approvals_service python manage.py migrate
```

#### Шаг 5: Проверьте, что бот запустился

```bash
# Проверьте логи бота
docker compose -f docker/docker-compose.yml logs bot_gateway

# Должно быть сообщение: "Bot is starting..."
```

#### Шаг 6: Протестируйте бота

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Бот должен ответить и начать диалог создания заявки

---

### Вариант 2: Запуск без Docker (для разработки)

#### Шаг 1: Установите зависимости

```bash
cd services/bot_gateway
pip install -r requirements.txt
```

#### Шаг 2: Убедитесь, что все сервисы запущены

Запустите все остальные сервисы на соответствующих портах:
- `requests_service` на порту 8000
- `categories_service` на порту 8001
- `approvals_service` на порту 8002
- `files_service` на порту 8100
- `reporting_service` на порту 8200

#### Шаг 3: Настройте переменные окружения

**Windows PowerShell:**
```powershell
$env:BOT_TOKEN="ваш_токен_от_BotFather"
$env:SERVICE_API_KEY="ваш_api_ключ"
$env:CATEGORIES_SERVICE_URL="http://localhost:8001/api"
$env:REQUESTS_SERVICE_URL="http://localhost:8000/api"
$env:FILES_SERVICE_URL="http://localhost:8100"
$env:APPROVALS_SERVICE_URL="http://localhost:8002/api"
$env:REPORTING_SERVICE_URL="http://localhost:8200"
```

**Linux/Mac:**
```bash
export BOT_TOKEN="ваш_токен_от_BotFather"
export SERVICE_API_KEY="ваш_api_ключ"
export CATEGORIES_SERVICE_URL="http://localhost:8001/api"
export REQUESTS_SERVICE_URL="http://localhost:8000/api"
export FILES_SERVICE_URL="http://localhost:8100"
export APPROVALS_SERVICE_URL="http://localhost:8002/api"
export REPORTING_SERVICE_URL="http://localhost:8200"
```

#### Шаг 4: Запустите бота

```bash
cd services/bot_gateway
python bot.py
```

Вы должны увидеть:
```
INFO: Bot is starting...
```

---

## 🔑 Необходимые ключи

### 1. BOT_TOKEN (обязательно)

**Что это:** Токен вашего Telegram-бота.

**Где получить:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` для создания нового бота
3. Следуйте инструкциям
4. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**📖 Подробная инструкция:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md#2-bot_token-telegram)

### 2. SERVICE_API_KEY (обязательно)

**Что это:** Ключ для аутентификации между сервисами.

**Как получить:**
```python
import secrets
print(secrets.token_urlsafe(32))
```

**Или используйте:** Любой строковый ключ (например, `my-secret-api-key-12345`)

**⚠️ ВАЖНО:** Этот же ключ должен быть установлен во всех сервисах!

### 3. URL-ы сервисов (опционально)

Если сервисы запущены в Docker, используются дефолтные значения:
- `CATEGORIES_SERVICE_URL=http://categories_service:8001/api`
- `REQUESTS_SERVICE_URL=http://requests_service:8000/api`
- `FILES_SERVICE_URL=http://files_service:8100`
- `APPROVALS_SERVICE_URL=http://approvals_service:8002/api`
- `REPORTING_SERVICE_URL=http://reporting_service:8200`

Если запускаете локально, укажите:
- `CATEGORIES_SERVICE_URL=http://localhost:8001/api`
- `REQUESTS_SERVICE_URL=http://localhost:8000/api`
- и т.д.

---

## ✅ Чеклист перед запуском

- [ ] Получен `BOT_TOKEN` от @BotFather
- [ ] Установлен `SERVICE_API_KEY` (тот же ключ во всех сервисах)
- [ ] Все сервисы запущены и доступны:
  - [ ] `requests_service` (порт 8000)
  - [ ] `categories_service` (порт 8001)
  - [ ] `approvals_service` (порт 8002)
  - [ ] `files_service` (порт 8100)
  - [ ] `reporting_service` (порт 8200)
- [ ] Применены миграции для Django-сервисов
- [ ] Установлены зависимости: `pip install -r services/bot_gateway/requirements.txt`

---

## 🚨 Частые проблемы

### Проблема: "BOT_TOKEN is not set"
**Решение:** Убедитесь, что переменная `BOT_TOKEN` установлена:
```bash
# Проверьте
echo $BOT_TOKEN  # Linux/Mac
$env:BOT_TOKEN   # Windows PowerShell
```

### Проблема: "Connection refused" при обращении к сервисам
**Решение:** 
1. Убедитесь, что все сервисы запущены
2. Проверьте URL-ы сервисов в переменных окружения
3. Проверьте, что порты не заняты другими приложениями

### Проблема: "401 Unauthorized" при обращении к сервисам
**Решение:**
1. Убедитесь, что `SERVICE_API_KEY` установлен в боте
2. Убедитесь, что тот же `SERVICE_API_KEY` установлен во всех сервисах
3. Проверьте, что ключи совпадают (без лишних пробелов)

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте логи бота на наличие ошибок
2. Убедитесь, что токен правильный (скопирован полностью)
3. Проверьте, что бот запущен (команда `/start` в BotFather показывает статус)
4. Убедитесь, что все сервисы доступны

---

## 📊 Зависимости бота

Бот зависит от следующих сервисов:

1. **categories_service** - для получения дерева категорий (склад → категория → подкатегория)
2. **requests_service** - для создания и управления заявками
3. **files_service** - для загрузки файлов из Telegram в Google Drive
4. **reporting_service** - для записи заявок в Google Sheets (опционально, через requests_service)

Все эти сервисы должны быть запущены и доступны перед запуском бота.

---

## 🔍 Проверка работоспособности

### 1. Проверьте доступность сервисов

```bash
# requests_service
curl http://localhost:8000/health/

# categories_service
curl http://localhost:8001/api/categories/tree/ -H "X-API-Key: ваш_ключ"

# files_service
curl http://localhost:8100/health

# reporting_service
curl http://localhost:8200/health
```

### 2. Проверьте логи бота

```bash
# Docker
docker compose -f docker/docker-compose.yml logs -f bot_gateway

# Локально
# Логи выводятся в консоль при запуске python bot.py
```

### 3. Протестируйте бота в Telegram

1. Найдите бота по username
2. Отправьте `/start`
3. Должен начаться диалог создания заявки
4. Пройдите все шаги:
   - Выбор склада
   - Выбор категории
   - Выбор подкатегории
   - Ввод суммы
   - Ввод комментария
   - Прикрепление файла
   - Подтверждение

---

## 📝 Пример .env файла для бота

```bash
# Обязательные
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
SERVICE_API_KEY=your-secret-api-key-here

# URL-ы сервисов (для Docker используйте имена контейнеров)
CATEGORIES_SERVICE_URL=http://categories_service:8001/api
REQUESTS_SERVICE_URL=http://requests_service:8000/api
FILES_SERVICE_URL=http://files_service:8100
APPROVALS_SERVICE_URL=http://approvals_service:8002/api
REPORTING_SERVICE_URL=http://reporting_service:8200

# Для локального запуска используйте localhost
# CATEGORIES_SERVICE_URL=http://localhost:8001/api
# REQUESTS_SERVICE_URL=http://localhost:8000/api
# FILES_SERVICE_URL=http://localhost:8100
# APPROVALS_SERVICE_URL=http://localhost:8002/api
# REPORTING_SERVICE_URL=http://localhost:8200
```

---

## 🎯 Минимальная конфигурация для тестирования

Если хотите просто протестировать бота без полной настройки:

```bash
# Минимальные переменные
BOT_TOKEN=ваш_реальный_токен_от_BotFather  # Реальный токен обязателен!
SERVICE_API_KEY=test-api-key-12345

# Остальные сервисы должны быть запущены с теми же ключами
```

**⚠️ ВАЖНО:** Для работы бота нужен **реальный BOT_TOKEN**, тестовый не подойдет, так как бот должен подключиться к Telegram API.

---

## 📚 Дополнительная информация

- **Документация по получению ключей:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md)
- **Быстрый старт тестов:** [tests/QUICK_START.md](tests/QUICK_START.md)
- **Основной README:** [README.md](README.md)

