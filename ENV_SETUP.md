# ⚙️ Настройка переменных окружения

## Быстрая настройка

1. **Скопируйте файл .env.example в .env:**
   ```bash
   # Windows PowerShell
   Copy-Item .env.example .env
   
   # Linux/Mac
   cp .env.example .env
   ```

2. **Откройте файл `.env` в текстовом редакторе**

3. **Заполните переменные, помеченные как "ОБЯЗАТЕЛЬНО УСТАНОВИТЬ":**
   - `BOT_TOKEN` - токен от @BotFather
   - `SERVICE_API_KEY` - ключ для межсервисной аутентификации

4. **Опционально заполните Google настройки** (если нужна работа с Google Sheets/Drive):
   - `GOOGLE_SHEET_ID`
   - `GOOGLE_SERVICE_ACCOUNT_JSON` или `GOOGLE_SERVICE_ACCOUNT_FILE`

## Структура .env файла

Файл `.env.example` содержит:

### 🔑 Обязательные ключи (требуется установить)
- `BOT_TOKEN` - токен Telegram-бота
- `SERVICE_API_KEY` - ключ для межсервисной аутентификации

### 📊 Google настройки (опционально)
- `GOOGLE_SHEET_ID` - ID Google таблицы
- `GOOGLE_SERVICE_ACCOUNT_FILE` или `GOOGLE_SERVICE_ACCOUNT_JSON` - ключ сервисного аккаунта
- `GOOGLE_CATEGORIES_SHEET` - имя листа категорий (по умолчанию: Categories)
- `GOOGLE_REPORTING_SHEET` - имя листа отчетов (по умолчанию: Reports)

### ⚙️ Остальные настройки (можно оставить по умолчанию)
- Настройки базы данных
- Django настройки
- URL-ы сервисов
- Дополнительные параметры

## Минимальная конфигурация

Для запуска бота достаточно заполнить только:
```bash
BOT_TOKEN=ваш_токен_от_BotFather
SERVICE_API_KEY=ваш_api_ключ
```

Остальное можно оставить по умолчанию.

## Подробные инструкции

- **Получение BOT_TOKEN:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md#2-bot_token-telegram)
- **Получение SERVICE_API_KEY:** Сгенерируйте любой строковый ключ
- **Получение Google ключей:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md#3-google_service_account_json)

## Безопасность

⚠️ **ВАЖНО:**
- Файл `.env` не должен попадать в Git (добавлен в `.gitignore`)
- Не публикуйте ключи в публичных местах
- Для продакшена используйте секреты Docker/CI






