# 📋 Что нужно для запуска бота

## Минимальные требования

### 1. BOT_TOKEN (обязательно, реальный токен) ⏱️ 5 минут

**Что это:** Токен вашего Telegram-бота от @BotFather.

**Где получить:**
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен

**⚠️ ВАЖНО:** Нужен **реальный токен**, тестовый не подойдет!

**📖 Подробно:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md#2-bot_token-telegram)

---

### 2. SERVICE_API_KEY (обязательно) ⏱️ 1 минута

**Что это:** Ключ для аутентификации между сервисами.

**Как получить:**
```python
import secrets
print(secrets.token_urlsafe(32))
```

**Или просто:** Используйте любой строковый ключ (например, `my-api-key-12345`)

**⚠️ ВАЖНО:** Этот же ключ должен быть установлен **во всех сервисах**!

---

### 3. Запущенные сервисы (обязательно)

Бот зависит от следующих сервисов (все должны быть запущены):

| Сервис | Порт | Что делает |
|--------|------|------------|
| `requests_service` | 8000 | Создание и управление заявками |
| `categories_service` | 8001 | Получение дерева категорий |
| `files_service` | 8100 | Загрузка файлов из Telegram |
| `approvals_service` | 8002 | Цепочки согласования |
| `reporting_service` | 8200 | Запись в Google Sheets |

**Как запустить:**
```bash
# Через Docker (рекомендуется)
docker compose -f docker/docker-compose.yml up -d

# Или локально (каждый сервис отдельно)
```

---

### 4. Примененные миграции (для Django-сервисов)

```bash
# requests_service
docker compose -f docker/docker-compose.yml exec requests_service python manage.py migrate

# categories_service
docker compose -f docker/docker-compose.yml exec categories_service python manage.py migrate

# approvals_service
docker compose -f docker/docker-compose.yml exec approvals_service python manage.py migrate
```

---

## Опциональные настройки

### URL-ы сервисов

Если сервисы запущены в Docker, используются дефолтные значения (не нужно настраивать).

Если запускаете локально, укажите:
```bash
CATEGORIES_SERVICE_URL=http://localhost:8001/api
REQUESTS_SERVICE_URL=http://localhost:8000/api
FILES_SERVICE_URL=http://localhost:8100
APPROVALS_SERVICE_URL=http://localhost:8002/api
REPORTING_SERVICE_URL=http://localhost:8200
```

### Google Sheets (для синхронизации категорий)

Если хотите синхронизировать категории из Google Sheets:
- `GOOGLE_SERVICE_ACCOUNT_JSON` - JSON ключ сервисного аккаунта
- `GOOGLE_SHEET_ID` - ID Google таблицы

**📖 Подробно:** [tests/SETUP_KEYS.md](tests/SETUP_KEYS.md)

---

## ✅ Чеклист перед запуском

- [ ] Получен `BOT_TOKEN` от @BotFather (реальный токен)
- [ ] Установлен `SERVICE_API_KEY` (тот же ключ во всех сервисах)
- [ ] Все сервисы запущены и доступны
- [ ] Применены миграции для Django-сервисов
- [ ] Установлены зависимости: `pip install -r services/bot_gateway/requirements.txt`

---

## 🚀 Быстрый запуск

**📖 [BOT_QUICK_START.md](BOT_QUICK_START.md)** - Запуск за 5 минут  
**📖 [BOT_SETUP.md](BOT_SETUP.md)** - Подробная инструкция

---

## Итого

| Требование | Обязательно | Время получения |
|-----------|-------------|------------------|
| BOT_TOKEN | ✅ Да | 5 минут |
| SERVICE_API_KEY | ✅ Да | 1 минута |
| Запущенные сервисы | ✅ Да | Зависит от способа запуска |
| Миграции | ✅ Да | 2-3 минуты |
| Google Sheets | ❌ Нет | 15-20 минут (опционально) |

**Минимальное время настройки:** ~10-15 минут (если сервисы уже запущены)






