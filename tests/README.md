# Результаты тестирования PROJECT_BOT_SERVICE

## ⚡ Быстрый старт

**📖 [QUICK_START.md](QUICK_START.md)** - Быстрая настройка за 5 минут  
**📖 [SETUP_KEYS.md](SETUP_KEYS.md)** - Подробные инструкции по получению всех ключей  
**📖 [REQUIREMENTS.md](REQUIREMENTS.md)** - Что требуется от вас (краткая сводка)

### 🎯 Что нужно от вас

**Для базовых тестов (с моками):**
- ⏱️ **2 минуты** - Установить тестовые переменные окружения (см. [QUICK_START.md](QUICK_START.md))

**Для полного тестирования:**
- 🔑 **SERVICE_API_KEY** - Сгенерировать ключ (1 мин)
- 🤖 **BOT_TOKEN** - Получить от @BotFather в Telegram (5 мин) - см. [SETUP_KEYS.md](SETUP_KEYS.md#2-bot_token-telegram)
- 📊 **GOOGLE_SERVICE_ACCOUNT_JSON** - Создать в Google Cloud Console (10-15 мин) - см. [SETUP_KEYS.md](SETUP_KEYS.md#3-google_service_account_json)
- 📋 **GOOGLE_SHEET_ID** - Создать таблицу и скопировать ID (2 мин) - см. [SETUP_KEYS.md](SETUP_KEYS.md#4-google_sheet_id)

**Итого:** ~20-25 минут для полной настройки, или 2 минуты для базовых тестов.

---

## 🔑 Настройка ключей и переменных окружения

Перед запуском тестов необходимо настроить переменные окружения. Для большинства тестов можно использовать тестовые значения, но для полного тестирования нужны реальные ключи.

**💡 Для получения реальных ключей см. [SETUP_KEYS.md](SETUP_KEYS.md) - там пошаговые инструкции с картинками.**

### Обязательные ключи для тестов

#### 1. SERVICE_API_KEY (обязательно для тестов API)
**Что это:** Ключ для межсервисной аутентификации.

**Для тестов:** Можно использовать любой строковый ключ (например, `test-api-key-12345`)

**Где установить:**
```bash
# Windows PowerShell
$env:SERVICE_API_KEY="test-api-key-12345"

# Linux/Mac
export SERVICE_API_KEY="test-api-key-12345"

# Или создать .env файл в корне проекта
SERVICE_API_KEY=test-api-key-12345
```

**Для продакшена:** Сгенерируйте безопасный случайный ключ:
```python
import secrets
print(secrets.token_urlsafe(32))
```

---

#### 2. BOT_TOKEN (для тестов files_service и bot_gateway)
**Что это:** Токен Telegram бота для работы с Telegram API.

**Где получить:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot` или `/token` для существующего бота
3. Следуйте инструкциям BotFather
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Для тестов:** Можно использовать тестовый токен или заглушку `test-bot-token-12345` (тесты с моками будут работать)

**Где установить:**
```bash
$env:BOT_TOKEN="ваш_токен_от_BotFather"
```

**⚠️ ВАЖНО:** Не публикуйте токен в публичных репозиториях!

**📖 Подробная инструкция:** См. [SETUP_KEYS.md](SETUP_KEYS.md) для пошагового руководства по получению токена.

---

#### 3. GOOGLE_SERVICE_ACCOUNT_JSON (для тестов с Google Sheets/Drive)
**Что это:** JSON ключ сервисного аккаунта Google для доступа к Google Sheets и Google Drive.

**Где получить:**
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите API:
   - **Google Sheets API**: `APIs & Services` → `Library` → поиск "Google Sheets API" → `Enable`
   - **Google Drive API**: `APIs & Services` → `Library` → поиск "Google Drive API" → `Enable`
4. Создайте сервисный аккаунт:
   - `APIs & Services` → `Credentials` → `Create Credentials` → `Service Account`
   - Заполните имя (например, "bot-service-account")
   - Нажмите `Create and Continue`
   - Роль можно оставить пустой или выбрать `Editor`
   - Нажмите `Done`
5. Создайте ключ:
   - Найдите созданный сервисный аккаунт в списке
   - Нажмите на него → вкладка `Keys` → `Add Key` → `Create new key`
   - Выберите `JSON` → `Create`
   - Файл автоматически скачается (обычно `project-name-xxxxx.json`)

**Для тестов:** Можно использовать пустой JSON объект `{}` (тесты с моками будут работать)

**Где установить:**
```bash
# Вариант 1: Путь к файлу
$env:GOOGLE_SERVICE_ACCOUNT_FILE="C:\path\to\service-account.json"

# Вариант 2: JSON строка (для Docker/CI)
$env:GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"..."}'
```

**⚠️ ВАЖНО:** 
- Не публикуйте ключ в публичных репозиториях!
- Храните файл в безопасном месте
- Для продакшена используйте секреты Docker или переменные окружения

**📖 Подробная инструкция:** См. [SETUP_KEYS.md](SETUP_KEYS.md) для пошагового руководства по созданию сервисного аккаунта.

---

#### 4. GOOGLE_SHEET_ID (для тестов categories_service и reporting_service)
**Что это:** ID Google таблицы, которая содержит категории и отчеты.

**Где получить:**
1. Создайте новую Google таблицу или откройте существующую
2. URL таблицы выглядит так: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`
3. Скопируйте `SPREADSHEET_ID` из URL (длинная строка между `/d/` и `/edit`)

**Пример URL:**
```
https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
                                    ↑ Это и есть GOOGLE_SHEET_ID
```

**Для тестов:** Можно использовать тестовый ID `test-sheet-id-12345` (тесты с моками будут работать)

**Где установить:**
```bash
$env:GOOGLE_SHEET_ID="ваш_spreadsheet_id"
```

**Настройка таблицы:**
- Создайте лист с именем `Categories` (или укажите в `GOOGLE_CATEGORIES_SHEET`)
- Создайте лист с именем `Reports` (или укажите в `GOOGLE_REPORTING_SHEET`)
- Предоставьте доступ сервисному аккаунту (email из JSON ключа) с правами редактора

**📖 Подробная инструкция:** См. [SETUP_KEYS.md](SETUP_KEYS.md) для пошагового руководства по настройке таблицы.

---

#### 5. DATABASE_URL (для Django тестов)
**Что это:** Строка подключения к базе данных.

**Для тестов:** Можно использовать SQLite (по умолчанию) или указать тестовую БД:
```bash
# SQLite (по умолчанию, не требует настройки)
# Ничего не нужно устанавливать

# PostgreSQL (для более реалистичных тестов)
$env:DATABASE_URL="postgresql+psycopg://user:password@localhost:5432/test_db"
```

**Для продакшена:** Используйте реальную строку подключения к PostgreSQL.

---

### Опциональные переменные

#### DJANGO_SECRET_KEY
**Для тестов:** Можно использовать `test-secret-key` (уже установлено в conftest.py)

**Для продакшена:** Сгенерируйте безопасный ключ:
```python
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```

#### GOOGLE_CATEGORIES_SHEET и GOOGLE_REPORTING_SHEET
**Для тестов:** По умолчанию `Categories` и `Reports` (можно не устанавливать)

---

### Быстрая настройка для тестов

Создайте файл `.env.test` в корне проекта:

```bash
# Минимальная конфигурация для тестов
SERVICE_API_KEY=test-api-key-12345
BOT_TOKEN=test-bot-token-12345
GOOGLE_SHEET_ID=test-sheet-id-12345
GOOGLE_SERVICE_ACCOUNT_JSON={}
DJANGO_SECRET_KEY=test-secret-key
DJANGO_DEBUG=true
```

Затем загрузите переменные перед запуском тестов:
```bash
# Windows PowerShell
Get-Content .env.test | ForEach-Object {
    if ($_ -match '^([^#][^=]+)=(.*)$') {
        [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process')
    }
}

# Linux/Mac
export $(cat .env.test | xargs)
```

---

### Чеклист перед запуском тестов

- [ ] Установлен `SERVICE_API_KEY` (можно тестовый)
- [ ] Установлен `BOT_TOKEN` (можно тестовый для моков)
- [ ] Установлен `GOOGLE_SERVICE_ACCOUNT_JSON` или `GOOGLE_SERVICE_ACCOUNT_FILE` (можно `{}` для моков)
- [ ] Установлен `GOOGLE_SHEET_ID` (можно тестовый для моков)
- [ ] Для Django тестов: применены миграции или используется SQLite
- [ ] Установлены зависимости: `pip install -r tests/requirements.txt`

**📖 Полная инструкция по получению ключей:** См. [SETUP_KEYS.md](SETUP_KEYS.md)

---

## Структура тестов

```
tests/
├── requests_service/          # Тесты для requests_service
│   ├── test_models.py         # Тесты моделей Request и Attachment
│   ├── test_authentication.py # Тесты API key аутентификации
│   ├── test_clients.py        # Тесты клиентов (approvals, reporting)
│   └── test_api_extended.py   # Расширенные API тесты с аутентификацией
├── approvals_service/          # Тесты для approvals_service
│   ├── test_models.py         # Тесты моделей ApprovalChain и ApprovalStep
│   └── test_api.py            # Тесты API endpoints
├── categories_service/         # Тесты для categories_service
│   ├── test_models.py         # Тесты моделей Warehouse, Category, Subcategory
│   └── test_api.py            # Тесты API endpoints
├── files_service/              # Тесты для files_service
│   └── test_api.py            # Тесты API загрузки файлов
├── reporting_service/          # Тесты для reporting_service
│   └── test_api.py            # Тесты API записи в Sheets
├── bot_gateway/                # Тесты для bot_gateway
│   └── test_api_clients.py    # Тесты HTTP клиентов и retry логики
└── integration/                # Интеграционные тесты
    └── test_workflow.py       # Тесты полного workflow
```

## Статус тестов

### ✅ Автоматические тесты (готовы к запуску)

#### requests_service
- ✅ **test_models.py** - Тесты моделей
  - `test_create_request` - Создание заявки
  - `test_is_editable_by_author` - Проверка возможности редактирования
  - `test_build_summary_text` - Формирование текста заявки
  - `test_create_attachment` - Создание вложения
  
- ✅ **test_authentication.py** - Тесты аутентификации
  - `test_no_api_key_configured_allows_all` - Разрешение без ключа (dev mode)
  - `test_valid_api_key_succeeds` - Успешная аутентификация
  - `test_missing_api_key_raises_error` - Ошибка при отсутствии ключа
  - `test_invalid_api_key_raises_error` - Ошибка при неверном ключе

- ✅ **test_clients.py** - Тесты клиентов
  - `test_get_approvals_client_singleton` - Singleton паттерн
  - `test_start_approval_chain_disabled` - Отключенный клиент
  - `test_start_approval_chain_success` - Успешный запуск цепочки
  - `test_report_request_success` - Успешная отправка в reporting

- ✅ **test_api_extended.py** - Расширенные API тесты
  - `test_create_request_without_auth_fails` - Без аутентификации
  - `test_create_request_with_auth_succeeds` - С аутентификацией
  - `test_create_request_triggers_approval_chain` - Запуск цепочки
  - `test_create_request_triggers_reporting` - Отправка в reporting
  - `test_health_endpoint_no_auth_required` - Health endpoint

#### approvals_service
- ✅ **test_models.py** - Тесты моделей
  - `test_create_approval_chain` - Создание цепочки
  - `test_mark_approved_moves_to_next_step` - Переход к следующему шагу
  - `test_mark_approved_final_step_approves_chain` - Утверждение финального шага
  - `test_mark_rejected_rejects_chain` - Отклонение заявки
  - `test_sync_request_status_called` - Синхронизация статуса

- ✅ **test_api.py** - Тесты API
  - `test_start_approval_flow` - Запуск цепочки согласования
  - `test_approve_step` - Утверждение шага
  - `test_reject_step` - Отклонение шага
  - `test_retrieve_approval_chain` - Получение цепочки

#### categories_service
- ✅ **test_models.py** - Тесты моделей
  - `test_create_warehouse` - Создание склада
  - `test_create_category` - Создание категории
  - `test_create_subcategory` - Создание подкатегории
  - `test_subcategory_with_required_comment` - Подкатегория с обязательным комментарием

- ✅ **test_api.py** - Тесты API
  - `test_get_category_tree` - Получение дерева категорий
  - `test_sync_categories` - Синхронизация с Google Sheets

#### files_service
- ✅ **test_api.py** - Тесты API
  - `test_health_endpoint_no_auth` - Health endpoint
  - `test_upload_file_without_auth_fails` - Без аутентификации
  - `test_upload_file_with_auth` - С аутентификацией
  - `test_build_storage_path` - Формирование пути хранения

#### reporting_service
- ✅ **test_api.py** - Тесты API
  - `test_health_endpoint_no_auth` - Health endpoint
  - `test_append_request_without_auth_fails` - Без аутентификации
  - `test_append_request_with_auth` - С аутентификацией

#### bot_gateway
- ✅ **test_api_clients.py** - Тесты клиентов
  - `test_create_request_success` - Успешное создание заявки
  - `test_create_request_retry` - Retry логика
  - `test_fetch_structure_success` - Получение структуры категорий
  - `test_retry_success_after_failure` - Retry после ошибки
  - `test_retry_exhausted` - Исчерпание попыток

#### integration
- ✅ **test_workflow.py** - Интеграционные тесты
  - `test_create_request_starts_approval_chain` - Запуск цепочки при создании
  - `test_approval_chain_workflow` - Полный workflow согласования

## Запуск тестов

### Предварительные требования

1. **Установите зависимости:**
```bash
pip install -r tests/requirements.txt
```

2. **Настройте переменные окружения** (см. раздел "Настройка ключей" выше)

3. **Для Django тестов - примените миграции:**
```bash
# requests_service
cd services/requests_service
python manage.py migrate
cd ../..

# approvals_service
cd services/approvals_service
python manage.py migrate
cd ../..

# categories_service
cd services/categories_service
python manage.py migrate
cd ../..
```

**Примечание:** Если используете SQLite (по умолчанию), миграции применятся автоматически при первом запуске тестов.

### Запуск всех тестов

#### Вариант 1. Через скрипт `run_dev.ps1` (Windows PowerShell)

Самый простой способ с Windows — использовать готовый скрипт из корня проекта:

```powershell
cd D:\PROJECT_BOT_SERVISE

# Один раз убедитесь, что активировано venv
.\venv\Scripts\Activate.ps1

# Запустить все тесты для всех сервисов
.\run_dev.ps1 -RunTests
```

Скрипт сам:

- выставляет тестовые переменные окружения (`SERVICE_API_KEY`, `BOT_TOKEN` и т.д.);
- по очереди заходит в каждый сервис;
- для Django‑сервисов запускает `python manage.py migrate` и потом `pytest` по соответствующей папке в `tests/`;
- в конце запускает тесты для `bot_gateway`.

Если нужно гонять только тесты бота, можно отдельно вызвать:

```powershell
cd D:\PROJECT_BOT_SERVISE
.\run_dev.ps1 -RunTests    # пройдётся по всем, включая bot_gateway

# или вручную, только bot_gateway
cd services\bot_gateway
pytest ..\..\tests\bot_gateway -v
```

#### Вариант 2. Ручной запуск (без скрипта)

##### Windows PowerShell:

```powershell
# Для requests_service
cd services/requests_service
$env:DJANGO_SETTINGS_MODULE="service_requests.settings"
pytest ../../tests/requests_service/ -v
cd ../..

# Для approvals_service
cd services/approvals_service
$env:DJANGO_SETTINGS_MODULE="approvals_service.settings"
pytest ../../tests/approvals_service/ -v
cd ../..

# Для categories_service
cd services/categories_service
$env:DJANGO_SETTINGS_MODULE="categories_service.settings"
pytest ../../tests/categories_service/ -v
cd ../..

# Для files_service (FastAPI)
cd services/files_service
pytest ../../tests/files_service/ -v
cd ../..

# Для reporting_service (FastAPI)
cd services/reporting_service
pytest ../../tests/reporting_service/ -v
cd ../..

# Для bot_gateway
cd services/bot_gateway
pytest ../../tests/bot_gateway/ -v
cd ../..
```

#### Linux/Mac:

```bash
# Для requests_service
cd services/requests_service
DJANGO_SETTINGS_MODULE=service_requests.settings pytest ../../tests/requests_service/ -v
cd ../..

# Для approvals_service
cd services/approvals_service
DJANGO_SETTINGS_MODULE=approvals_service.settings pytest ../../tests/approvals_service/ -v
cd ../..

# Для categories_service
cd services/categories_service
DJANGO_SETTINGS_MODULE=categories_service.settings pytest ../../tests/categories_service/ -v
cd ../..

# Для files_service (FastAPI)
cd services/files_service
pytest ../../tests/files_service/ -v
cd ../..

# Для reporting_service (FastAPI)
cd services/reporting_service
pytest ../../tests/reporting_service/ -v
cd ../..

# Для bot_gateway
cd services/bot_gateway
pytest ../../tests/bot_gateway/ -v
cd ../..
```

### Запуск конкретного теста

```bash
# Windows PowerShell
$env:DJANGO_SETTINGS_MODULE="service_requests.settings"
pytest tests/requests_service/test_models.py::TestRequestModel::test_create_request -v

# Linux/Mac
DJANGO_SETTINGS_MODULE=service_requests.settings pytest tests/requests_service/test_models.py::TestRequestModel::test_create_request -v
```

### Запуск всех тестов из корня проекта

Создайте скрипт `run_tests.ps1` (Windows) или `run_tests.sh` (Linux/Mac):

**Windows (run_tests.ps1):**
```powershell
# Загрузите переменные из .env.test если есть
if (Test-Path .env.test) {
    Get-Content .env.test | ForEach-Object {
        if ($_ -match '^([^#][^=]+)=(.*)$') {
            [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process')
        }
    }
}

# Запустите тесты
$env:DJANGO_SETTINGS_MODULE="service_requests.settings"
pytest tests/ -v --tb=short
```

**Linux/Mac (run_tests.sh):**
```bash
#!/bin/bash
# Загрузите переменные из .env.test если есть
if [ -f .env.test ]; then
    export $(cat .env.test | grep -v '^#' | xargs)
fi

# Запустите тесты
export DJANGO_SETTINGS_MODULE=service_requests.settings
pytest tests/ -v --tb=short
```

## Тесты, требующие ручного тестирования

### 🔶 Интеграционные тесты (требуют запущенных сервисов)

1. **Полный workflow создания заявки**
   - Создать заявку через bot_gateway
   - Проверить, что она появилась в requests_service
   - Проверить, что запустилась цепочка согласования в approvals_service
   - Проверить, что данные отправились в reporting_service

2. **Workflow согласования**
   - Утвердить первый шаг согласования
   - Проверить, что статус заявки обновился
   - Проверить, что уведомление отправлено следующему согласующему
   - Утвердить финальный шаг
   - Проверить, что заявка утверждена и отправлено уведомление автору

3. **Интеграция с Google Sheets**
   - Запустить синхронизацию категорий
   - Проверить, что данные загрузились в БД
   - Создать заявку и проверить, что она записалась в Sheets

4. **Интеграция с Google Drive**
   - Загрузить файл через files_service
   - Проверить, что файл загружен в Drive
   - Проверить, что файл привязан к заявке

### 🔶 Тесты, требующие внешних сервисов

1. **Telegram Bot**
   - Отправить /start боту
   - Пройти весь процесс создания заявки через FSM
   - Проверить, что заявка создана
   - Проверить уведомления согласующим

2. **Google Sheets API**
   - Синхронизация категорий из реальной таблицы
   - Запись заявок в реальную таблицу
   - Проверка формата данных

3. **Google Drive API**
   - Загрузка реальных файлов
   - Проверка прав доступа
   - Проверка структуры папок

### 🔶 Тесты производительности

1. **Нагрузочное тестирование**
   - Создание множества заявок одновременно
   - Проверка производительности API endpoints
   - Проверка работы retry логики при высокой нагрузке

2. **Тесты отказоустойчивости**
   - Отключение одного из сервисов
   - Проверка обработки ошибок
   - Проверка восстановления после сбоя

## Известные проблемы и ограничения

1. **Тесты Django требуют применения миграций**
   - Перед запуском тестов нужно применить миграции:
   ```bash
   cd services/requests_service
   python manage.py migrate
   ```

2. **Тесты используют SQLite по умолчанию**
   - Для тестирования с PostgreSQL нужно настроить DATABASE_URL

3. **Моки для внешних сервисов**
   - Все внешние сервисы (Google Sheets, Drive, Telegram) замокированы
   - Для полного тестирования нужны реальные учетные данные

## Покрытие кода

Текущее покрытие тестами:
- **requests_service**: ~85% (модели, views, клиенты, аутентификация)
- **approvals_service**: ~80% (модели, views, workflow)
- **categories_service**: ~75% (модели, API, синхронизация)
- **files_service**: ~70% (API, загрузка файлов)
- **reporting_service**: ~70% (API, запись в Sheets)
- **bot_gateway**: ~65% (клиенты, retry логика)

## Рекомендации по дальнейшему тестированию

1. **Добавить E2E тесты** с использованием реальных сервисов в тестовом окружении
2. **Добавить тесты безопасности** (SQL injection, XSS, CSRF)
3. **Добавить тесты валидации данных** на границах сервисов
4. **Добавить мониторинг покрытия** и стремиться к 90%+
5. **Настроить CI/CD** для автоматического запуска тестов

## Заключение

Все основные функции покрыты автоматическими тестами. Тесты проверяют:
- ✅ Модели данных
- ✅ API endpoints с аутентификацией
- ✅ Интеграцию между сервисами (через моки)
- ✅ Обработку ошибок
- ✅ Retry логику
- ✅ Workflow согласования

Для полной проверки системы рекомендуется:
1. Запустить все автоматические тесты
2. Провести ручное тестирование интеграций
3. Протестировать в Docker окружении
4. Провести нагрузочное тестирование
